services:
  jpetstore:
    image: example/jpetstore:fixture
    command: java -jar /app/jpetstore.jar
    ports:
      - "8080:8080"
    depends_on:
      - mysql
  mysql:
    image: mysql:8
    volumes:
      - mysql-data:/var/lib/mysql
volumes:
  mysql-data:
