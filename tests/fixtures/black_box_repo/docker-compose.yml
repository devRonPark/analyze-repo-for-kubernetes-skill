services:
  api:
    build: ./services/api
    command: npm start
    ports:
      - "8080:8080"
    environment:
      DATABASE_URL: postgres://app:app@postgres:5432/app
  postgres:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: app
