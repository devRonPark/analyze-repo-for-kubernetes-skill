name: Test Skill

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Validate package
        run: python3 scripts/validate_skill.py .
      - name: Run core tests
        run: python3 -m unittest tests.test_codex_target_gate_hook tests.test_package tests.test_repository_evidence tests.test_trigger_precision_eval -v
